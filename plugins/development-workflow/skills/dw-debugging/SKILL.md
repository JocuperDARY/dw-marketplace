---
name: dw-debugging
description: 诊断与调试方法论：量化验证方法目录、中间态信号检查、反向验证法（诊断先行）、闭环验证（正/负反馈回路）、全生命周期逐级排查、常见实现错误分类（归因优先级P1→P2→P3）、管道未激活检查、实验登记表、版本回退策略。触发词：调试、信号诊断、中间态、闭环、生命周期、排查、管道、pipeline、反向验证、未激活、debugging、signal、lifecycle、逐级排查。
---

# 诊断与调试方法论

> 本 skill 是 [development-workflow](../development-workflow/SKILL.md) 的子模块，覆盖 **核心方法中的诊断与调试技术**。

---

## 量化验证方法目录

### 性能类

| 方法 | 适用场景 | 工具 |
|------|---------|------|
| FPS 对比 | 速度优化 | `time`、`cProfile`、自定义计时 |
| 内存 profiling | 内存优化 | `memory_profiler`、`tracemalloc` |
| CPU/GPU 利用率 | 资源利用 | `nvidia-smi`、`py-spy` |
| 延迟分位数 | 实时系统 | P50/P95/P99 统计 |

### 精度类

| 方法 | 适用场景 | 说明 |
|------|---------|------|
| 数值对比（MSE/MAE） | 回归/滤波 | 与旧版逐帧数值差异 |
| 指标对比（F1/准确率/吞吐量等） | 项目核心指标 | 同口径同数据同条件对比 |
| A/B 测试 | 参数调优 | 一次改一个参数 |
| 消融实验 | 组件评估 | 逐个开关组件量化贡献 |

### 信号级诊断

| 方法 | 适用场景 | 说明 |
|------|---------|------|
| 中间态信号检查 | 复合系统 | 检查管道是否产生预期中间量（而非只看最终指标） |
| 逐组件隔离验证 | 管道型系统 | 每个子组件独立测试其输入输出 |
| 诊断可视化 | 模式识别 | 将关键信号绘制成图表，识别肉眼难察的模式 |

### 跨数据集交叉验证

| 维度 | 验证内容 | 操作 |
|------|---------|------|
| 场景覆盖 | 不同特征的输入数据 | 至少覆盖 2-3 个特征差异较大的场景 |
| 预期行为验证 | 验证各场景上的行为是否符合预期 | 场景 A 不触发、场景 B 触发、场景 C 部分触发 |
| 退化检测 | 改动了场景 A 的逻辑但只在场景 B 上测 | 所有受影响的场景都要回测 |

---

## 中间态信号检查

实践中最容易犯的错误是只盯着最终指标，忽略了中间管道是否产生了正确的信号。

| 阶段 | 最终指标 | 中间态信号 | 本应发现的问题 |
|------|---------|-----------|-------------|
| 初版实现 | 无提升 | `feature_enabled=0/377(0%)` | 新功能从未激活——死代码 |
| 第 2 版 | 无提升 | `feature_enabled=0`, `input_ready=0` | 参数未传入核心函数 |
| 第 3 版 | 无提升 | `feature_enabled=120/377(32%)`, `input_ready=205` | 管道活了！但为什么结果全零？|
| 第 4 版 | 无提升 | 中间量全零且无错误日志 | 中间量从未被赋值——诊断代码缺失 |

### 信号检查清单

```
□ 管道是否被调用？（增加 print/日志确认入口执行）
□ 输入是否正确？（检查输入数据内容，而非 shape/type）
□ 中间量是否合理？（范围、分布、趋势）
□ 输出是否被正确写回？（下游是否读取了预期字段）
□ 最终指标是否变化？
```

> **原则**：中间态信号是管道内第一响应者。最终指标是滞后的。优先检查信号，而非指标。

---

## 反向验证法：诊断先行

最有效的验证方式不是"看代码对不对"，而是**设置诊断变量 → 运行 → 查看诊断输出 → 反向推导组件是否正常运行**。

```
代码审查模式：
  代码是对的 → 代码应该产生 X 结果 → 运行验证 → 结果不对 → 重新审查代码
  （假设前提：你能通过看代码判断它是否正确）

反向验证模式：
  设置诊断变量 → 运行 → 看产出 →
    ├─ 结果显示预期信号 → 管道正常 ✅
    └─ 结果显示异常信号 → 追查具体层级
  （不依赖对代码正确性的预判——让数据说话）
```

### 实施步骤

```
□ 步骤 1：确定关键诊断信号（什么中间量能直接反映组件是否在工作？）
□ 步骤 2：确保诊断代码完整（每个代码路径都要写入诊断变量）
□ 步骤 3：构建可视化（分布直方图、时序折线图）
□ 步骤 4：建立预期基准（不同场景下定义"正常"的中间量范围）
□ 步骤 5：迭代验证（改一次代码 → 跑诊断 → 看输出 → 确认行为符合预期）
```

### 诊断变量设计原则

| 原则 | 说明 | 反例 |
|------|------|------|
| **每个路径都记录** | 函数的所有返回路径都必须设置诊断 | 提前 `return` 不写诊断变量 |
| **用值域可解释的量** | 用有物理意义的量（百分比、计数、比率） | 复杂的内部状态叠起来无法解读 |
| **时间序列优先** | 按时序记录，可以画折线图观察趋势 | 只输出均值会掩盖中间波动 |
| **区分"未激活"和"激活后为零"** | 用两个不同信号表示 | `enabled=0` vs `enabled=1, output=0` |
| **极端情况下也有输出** | 即使出错也记录错误原因 | 静默崩溃 → 下游不知道发生了什么 |

---

## 闭环验证

验证不是一次性的——它应是一个闭环：**设计检测指标 → 设置捕获点 → 运行 → 检查信号 → 如果异常（负反馈）→ 从链路起点重新排查 → 修复 → 再运行**。

### 两种反馈回路

| 回路 | 触发条件 | 响应 | 目的 |
|------|---------|------|------|
| **正反馈**（确认） | 信号在预期范围内 | 继续到下一阶段 | 保证进度向前 |
| **负反馈**（修正） | 信号偏离预期 | 回到链路起点重新排查 | 防止 bug 累积 |

**负反馈回路是质量的核心。** 发现异常 → 必须追溯到根因 → 修复 → 再运行确认。

---

### 与信号检查的区别

```
信号检查：      查找关键信号 → 确定异常区域 → 修复（目标：找到异常）
生命周期排查：  明确数据链路 → 每级捕获输入/输出 → 逐级验证
               └─ 目标不是"找到异常"，而是"每级都正常"才往下走
```

---

## 全生命周期逐级排查

从数据管道的起点到终点，每级验证后才进入下一级：

```
逐级验证顺序：
  输入创建 → 数据提取 → 合并/变换 → 质量过滤 → 核心算法 → 结果消费
      ↓          ↓          ↓          ↓          ↓          ↓
   类型正确?   提取量达标?  合并率正常?  过滤率合理?  输出非零?   下游可见?
```

关键原则：
1. **每级必须有捕获点**——排查阶段每级至少有一个 `print`/`log` 确认输入输出
2. **捕获点在开发阶段就设置**——不要等出了问题再想在哪捕获
3. **"正常"是一个范围，不是固定值**

---

### 闭环排查示例

当中间信号异常时，**绝不允许跳级排查**。一次真实案例中，一个异常信号（输出全零）被错误归因为"方案本身不适用"（P3），跳过了藏在链路中的 4 个实现 bug（P1）。

正确流程：
```
输出全零 → 负反馈触发
  └─ 级 1：输入数据是否存在？→ 是（200+ 条）
  └─ 级 2：数据过滤掩码是否正确？→ 不对！新数据未过滤
      └─ bug 1：补数方法没更新掩码 → 修复
  └─ 再运行 → 输出有值但偏小
  └─ 级 3：统计字段存了什么？→ 子集数，不是总数
      └─ bug 2：赋值语义错误 → 修复
  └─ 再运行 → 输出正常
  └─ 级 4：状态标记 → 只有默认值
      └─ bug 3：回退路径没写标记 → 修复
  └─ 级 5：异常路径 → 4 条遗漏诊断
      └─ bug 4-6：补充赋值
  └─ 全部修复 → 验证通过
```

**核心教训**：多次实践中，最终指标异常时直接跳到「方案本身不适用」的结论，跳过了藏在链路中的多个实现 bug。正确做法是先假设实现有错，逐级排查。

---

## 常见实现错误分类

**归因优先级**：当结果异常时，按以下优先级逐级排查——绝不允许跳级：

```
优先级 1：实现错误（90% 的异常来自此类）
   ├─ 传参错误（调用链中参数被默认值覆盖）
   ├─ 索引/边界错误（数组越界、守卫条件失效）
   ├─ 条件/逻辑错误（and/or 混用、分支条件写反）
   ├─ 维度/类型错误（shape 不匹配、float32 vs float64）
   ├─ 累积/状态错误（状态未在关键帧/重置点清零）
   └─ 诊断遗漏（功能实现了但诊断字段未赋值 → 输出全零）

优先级 2：方案理解偏差
   └─ 方案依赖的假设在当前场景是否成立？

优先级 3：方案本身不适用
   └─ 只有彻底排除 1 和 2 后才能考虑「方案不行」
```

| 类别 | 典型表现 | 检查方法 |
|------|---------|---------|
| 传参错误 | 参数被默认值覆盖、参数名不匹配 | 逐层打印实际接收的参数值 |
| 索引/边界错误 | 数组越界、长度变化后索引失效 | 检查守卫条件 + 边界值测试 |
| 条件/逻辑错误 | `and`/`or` 混用、取反遗漏 | 分支覆盖测试 |
| 维度/类型错误 | shape 不匹配、float32 vs float64 | 打印 `.shape` + `.dtype` |
| 累积/状态错误 | 帧间状态未在重置点清零 | 检查重置逻辑 |
| 诊断遗漏 | 功能实现了但诊断字段未赋值 | 搜索诊断输出字段的每个写入点 |
| 路径/配置错误 | 路径拼接、配置键名拼写 | 运行前打印配置解析结果 |

---

## 管道未激活检查

当怀疑某段代码"没有被执行"时：

```
□ 入口条件是否满足？（if guard、配置开关、hasattr 检查）
□ 函数是否被显式调用？（搜索函数名是否存在调用点）
□ 参数是否被正确传递？（从调用链最外层逐层检查）
□ 返回值是否被接收或使用？
□ 诊断代码是否完整？
```

> **可悲的真相**：90% 的「方案无效」其实是「方案根本没运行」。

---

## 实验登记表

| # | 方向 | 假设 | 最小改动 | 实测指标 | 关键信号 | 判决 | 放弃原因 |
|---|------|------|---------|---------|---------|------|---------|
| 1 | 方案 A | 假设描述 | ~80 行 | 0.69 | enabled=32%, output=0 | ❌ | 方案未实际生效 |
| 2 | 方案 B | 假设描述 | ~30 行 | 0.68 | 信号噪声大 | ❌ | 输入质量不满足假设 |
| 5 | 方案 E | 假设描述 | ~3 行 | 0.69 | 无退化 | ✅ | 零开销，无害 |

**关键教训**：每次尝试都记录判决和理由。避免在 6 个月后回来发现有人又在试相同的方案。

---

## 版本回退策略

```
□ 定位有问题的提交范围（git log --oneline）
□ 确定回退粒度（整个提交？还是只回退特定文件？）
  └─ `git show <commit> --stat` 查看改动范围
□ 选择回退方式：
   方法 A: `git revert <commit>` — 保留历史，适合多人协作
   方法 B: `git checkout <commit>^ -- <file>` — 只回退特定文件
□ 保留有问题的代码的分析结论供后续参考
```

---

## 相关子Skill

- [dw-diagnosis](../dw-diagnosis/SKILL.md) — 诊断阶段（证据收集、根因分析）
- [dw-verification](../dw-verification/SKILL.md) — 三C验证与实测驱动调试
- [dw-optimization](../dw-optimization/SKILL.md) — 性能优化方法论
- [development-workflow](../development-workflow/SKILL.md) — 返回总纲
